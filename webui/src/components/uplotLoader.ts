/** Loaded on demand (see UPlotChart): the chart library and its CSS land in
 *  their own chunk, fetched only once a chart is on screen. */
import uPlot from "uplot";
import "uplot/dist/uPlot.min.css";

export default uPlot;
